<?php
    
 
    // $connection = odbc_connect("ADA", "", "");
    // if ($connection){
    // 	echo "Yes, were connected!\n";
    // }
    // else{
    //     echo(json_encode(array('outcome' => false, 'message' => 'Unable to connect')));

    // }

    // $sql = "SELECT Company FROM Customer";
    // $rs = odbc_exec($connection, $sql);
    // while(odbc_fetch_row($rs)){
    //     $name = odbc_result($rs, 'Company');
    //     echo "$name"."\n";
        
    // }



    // $tablelist = odbc_tables($connection);

    // $query = "SELECT CLIENTE FROM D_CLIE WHERE CLIENTE ='30207552';";
    // echo $query;

    // $rs = odbc_exec($connection, $query);

    // while(odbc_fetch_row($rs)){
    // 	$name = odbc_result($rs, 'CLIENTE');

    // 	echo "$name"."\n";
    	
    // }

?>